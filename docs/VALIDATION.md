# 公开包验证记录

日期：2026-09-09。环境：macOS、Python 3.11.15、Node.js 26.5.0。CI 另验证 Python 3.11 / Node.js 22。

以下检查针对独立公开目录，不将私人生产测试计数混入。首次远端检查发现 npm 安装路径假设，已增加精确只读范围和反例；后续发现 Chrome 测试未等待子进程退出便删除临时目录，已改为等待退出，未删除布局断言：

- 后端：全新本地虚拟环境 804 条通过；新增外置 npm 沙箱反例后，GitHub macOS 检查通过完整后端测试。包含公开入口注册幂等与独立 CLI 启动。
- 前端：791 条通过，包括隔离 Chrome 物理布局与原生主页挂载/关闭清理。
- 从空 Python 虚拟环境安装固定直接依赖；`npm ci --ignore-scripts` 安装前端锁定依赖，构建输出严格为三个插件文件。
- 离线演示：零网络、零模型；同一知识 ID 保存两个版本，旧建议仍可读，目录可搜索，Agent 使用步骤可读取。
- 独立回环端口服务启动，并通过知识目录 API 读取演示数据。
- 发布集合检查：不带源仓库 Git 历史、个人 Vault、生产 DB、`.env`、原部署备份或登录材料。密钥格式与已知个人路径扫描未发现命中；测试中的私网地址和 URL userinfo 是反例夹具。

**这些不证明：** 所有真实输入都能回答、不同用户环境已普遍通过、已经稳定无人值守一个自然月，或系统能够自行修改代码。真实来源会变化；外部 Agent 接入和新机器长期反馈仍是接下来的验收重点。

运行方法见 [QUICKSTART.md](QUICKSTART.md) 和仓库 Actions。macOS 沙箱与 Chromium 测试依赖操作系统能力；没有浏览器的环境会明确跳过对应物理布局检查，不应将跳过写成通过。
